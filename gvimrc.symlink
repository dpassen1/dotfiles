if has("gui_gtk2")
    set guifont=PragmataPro\ Mono\ Liga\ 11
end

if has("gui_macvim")
    set macligatures
    set guifont=PragmataPro\ Liga:h11
end

if has("gui_running")
    set guioptions=e-t
endif

set showtabline=2
