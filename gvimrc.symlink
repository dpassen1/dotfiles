if has("gui_gtk2")
    set guifont=PragmataPro\ 11
end

if has("gui_macvim")
    set macligatures
    set guifont=PragmataPro:h11
end

if has("gui_running")
    set guioptions=e-t
endif

set showtabline=2
